import { BUTTON_TYPE_SUBMIT } from "../constants/button-types";
import { BUTTON_LABEL_SUBMIT } from "../content/button-labels";

export const submitButton = {
    type: BUTTON_TYPE_SUBMIT,
    label: BUTTON_LABEL_SUBMIT,
    name: "button",
    disabled: false
}